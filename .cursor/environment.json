{
	"$schema": "https://www.cursor.com/schemas/environment.schema.json",
	"name": "topcredit",
	"install": "pnpm install && pnpm exec playwright install chromium --with-deps",
	"ports": [
		{
			"name": "Next.js",
			"port": 3000
		}
	],
	"terminals": [
		{
			"name": "Dev",
			"command": "pnpm dev",
			"description": "Next.js dev server (pnpm)."
		}
	]
}
